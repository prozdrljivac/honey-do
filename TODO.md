# Tasks

- Add CORS to the API Gateway setup, CORS should be added accross the api points for all methods
- Fix an issue on FE where task is created 201, but FE sees it as error.
- Make a functional list tasks api endpoint
- Make a funcitonal get task details endpoint
- Create a testing environment
